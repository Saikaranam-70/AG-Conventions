export type Hall = {
  id: string;
  name: string;
  description: string;
  capacity: number;
  price: number;
  imageId: string;
};

export type Testimonial = {
  id: string;
  quote: string;
  author: string;
  title: string;
  imageId: string;
};
